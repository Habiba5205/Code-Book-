namespace CodeBook.Data-Acces-Layer
{
public enum NotificationType { like, comment, reaction, postRemoval, postAcceptance}
public enum Role { admin, member}
public enum PostStatus { pending, accepted, rejected}
}